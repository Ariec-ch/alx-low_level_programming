#include <stdio.h>

/**
 * main -  a program that prints the digital numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
	int b;

	for (b = 0 ; b < 10 ; b++)
		putchar(b);
	putchar('\n');
	return (0);
}
