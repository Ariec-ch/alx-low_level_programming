#include <stdio.h>

/**
 * main -  a program that prints the digital numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
	short int a;

	for (a = 0 ; a < 10 ; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
